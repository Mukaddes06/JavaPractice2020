package Notes;

public class Array {
    public static void main(String[] args) {
        /*
        Array is an onject that can hold multiple items ,
        elements of same array has fixed size
         Once created with certain size, it can not be changed
         We can change the value inside the array
         Default value for array item if its is empty

         byte short int long --->> 0
         float double --->> 0.0;
         char --->> ' ' empty caharacter
         boolean ---->> false

         3 ways to create an array object and assign it to variable
         short[] myNumbers= new shoet[4];  --->> 4 is item count
         //we asiign the value using array variable and its index

         myNumbers[0] = 10;
         myNumbers[1] = 40;
         myNumbers[2] = 30;
         myNumbers[3] = 7;

         How do we get the size of an array
         int size = myNumbers.length ; //4
         int lastIndex = size -1 ; myNumbers.length-1;
         // How do we get last item of any array
         // I want to save last item into a variable
        // short lastItemValue = myNumbers[3];
        // short lastItemValue = myNumbers[3lastIndex];

        short lastItemValue = myNumbers[myNumbers.length-1];
        // print third item
        print (myNumbers[2]);

        // 1.1 want to add 1 to the second item value
        numbers[1] = myNumbers[1] +1; // 41

       // 1.2 I want to double the second item value
       myNuumbers[1] = myNumbers[1] * 2; // 82

       //1.3 I want to assign 3 item value to

       // sum of first and second item

       // Just change my int array data type to int to avoid issues

       // 1.4 Swap the first item value with last item value


         */
    }
}
